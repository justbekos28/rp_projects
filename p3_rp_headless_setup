Raspberry Pi 4B - Headless Setup

** Load ssh & wpa_supplicant.conf files to sd card (see sample files in main folder)

change default password:
passwd

Raspbian Lite Packages Updates pull down:
sudo apt update

Raspbian Lite Upgrade:
sudo apt full-upgrade

